services:
  fastapi:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app

  ngrok:
    image: wernight/ngrok
    depends_on:
      - fastapi
    environment:
      - NGROK_AUTH=2nttMVJBCP1PPbKPOaMuU8P5KsS_4K5feUDnvFdTYye4wFj5C
      - NGROK_PORT=fastapi:8000
    ports:
      - "4040:4040"

  # nginx:
  #   image: nginx:latest
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/conf.d/default.conf
  #   ports:
  #     - "80:80"
  #   depends_on:
  #     - fastapi
